server:
  port: 8001

spring:
  application:
    name: server-eureka

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8001/eureka/
    fetch-registry: false
    register-with-eureka: false