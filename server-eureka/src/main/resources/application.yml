server:
  port: 7001

spring:
  application:
    name: server-eureka

eureka:
  client:
    service-url:
      defaultZone: http://localhost:7001/eureka/
    fetch-registry: false
    register-with-eureka: false